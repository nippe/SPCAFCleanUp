DisplayNameAdder
================

Simple little application that traverses a SharePoint solutions xml-files and adds the DisplayName attribute and gives it the same values as Name. This is a warning in SPCAF so I'm doing this to remove some errors from the report. Written in Visual Studio 2012
